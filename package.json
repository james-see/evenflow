{
  "name": "evenflow",
  "version": "0.1.0",
  "description": "CLI tool that scaffolds a CMS-powered static site with SQLite WASM + OPFS — deployable to GitHub Pages with zero server cost.",
  "type": "module",
  "license": "AGPL-3.0",
  "bin": {
    "evenflow": "./bin/evenflow.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "3.53.0-build1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "dist",
    "src/template"
  ]
}